name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: self-hosted

    steps:
    
    - name:  build
      working-directory: test
      run: cpp main.cpp -o compiled_Hello_World
    
